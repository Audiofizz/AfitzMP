using System;
using System.Collections.Generic;
using System.Text;

namespace GameServer.Utilities
{
/*    static class Create
    {
        public static void PhysicsObject(PhysicsObject physicsObject)
        {
            physicsObject.index = Server.physicsObjects.Count;
            if (!Server.physicsObjects.TryAdd(Server.physicsObjects.Count, physicsObject))
                PhysicsObject(physicsObject, physicsObject.index + 1);
        }

        private static void PhysicsObject(PhysicsObject physicsObject,int index)
        {
            physicsObject.index = index;
            if (!Server.physicsObjects.TryAdd(index, physicsObject))
                PhysicsObject(physicsObject, physicsObject.index + 1);
        }
    }*/
}
